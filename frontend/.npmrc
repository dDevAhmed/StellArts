# Prevents third-party packages (like @trezor) from running problematic post-install scripts
# that depend on external tools like Yarn being installed on the contributor's machine.
ignore-scripts=true
